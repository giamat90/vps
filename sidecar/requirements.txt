--extra-index-url https://download.pytorch.org/whl/cpu
# demucs 4.0.1 (last PyPI release, unmaintained since 2023) calls torch.load()
# without weights_only=False; torch>=2.6 flipped that default to True, which
# breaks model loading entirely — every audio file fails, not just some.
# Pin below the behavior change until upstream demucs is fixed/replaced.
torch<2.6
torchaudio<2.6
demucs
torchcrepe
librosa
soundfile
numpy
scipy
# YouTube periodically breaks compatibility with older yt-dlp clients
# (nsig extraction / bot-detection changes). The installed app runs a
# PyInstaller-frozen sidecar with whatever version was resolved at build
# time — it never self-updates. Bump this floor whenever YouTube import
# starts failing in production, then rebuild the sidecar and cut a release.
yt-dlp>=2026.7.4
pywin32; sys_platform == "win32"
